{
  "name": "with-tailwind",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "cf-typegen": "turbo run cf-typegen",
    "test": "turbo run test",
    "preview": "turbo run preview",
    "deploy": "turbo run deploy",
    "deploy:prod": "turbo run deploy:prod",
    "deploy:preview": "turbo run deploy:preview",
    "deploy:stage": "turbo run deploy:stage",
    "build:opennext": "turbo run build:opennext",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:push": "turbo -F web-app db:push",
    "db:pull": "turbo -F web-app db:pull",
    "db:studio": "turbo -F web-app db:studio",
    "db:generate": "turbo -F web-app db:generate",
    "db:migrate": "turbo -F web-app db:migrate"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "turbo": "^2.5.5"
  },
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=22"
  }
}
